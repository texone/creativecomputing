package cc.creativecomputing.gl;



public class GLShaderProgramTest{// extends TestCase{
	/*
	private static class GLTestApp extends CCAppProgrammable{
		
	}
	
	private static GLTestApp _myApp;
	
	@BeforeClass
	public static void createInitApp(){
		
	}

	@Test
	public void testActiveAttributes() {
		// preset ..
        
		try {
			final NEWTGLContext.WindowContext winctx = NEWTGLContext.createOffscreenWindow(new GLCapabilities(GLProfile.getGL2ES2()), 480, 480, true);
			CCLog.info("YO" + _myApp);
			GLShaderProgram myProgram = new GLShaderProgram();
			assert(true);
		}
		catch (Exception e) {
			System.out.println("FAIL");
			fail("[FAILED] "+e.getMessage());
		}
	}*/
}
